# Web framework + ASGI server for the GenAI microservice.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# Settings / validation.
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# Cloud backend (OpenAI / any OpenAI-compatible API).
openai>=1.30,<2.0

# Local / self-hosted backend (LLaMA-family models served by Ollama).
ollama>=0.3,<1.0
