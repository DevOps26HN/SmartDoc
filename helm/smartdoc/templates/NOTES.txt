SmartDoc has been deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Components:
  - client  ({{ include "smartdoc.client.fullname" . }})  -> exposed via Ingress
  - server  ({{ include "smartdoc.server.fullname" . }})  -> internal (ClusterIP), reached via the client /api proxy
  - db      ({{ include "smartdoc.db.fullname" . }})       -> internal (ClusterIP), not exposed

{{- if .Values.ingress.enabled }}

The system is reachable from the public internet at:

  http://{{ .Values.ingress.host }}/

  UI:  http://{{ .Values.ingress.host }}/
  API: http://{{ .Values.ingress.host }}/api/v1/documents
{{- else }}

Ingress is disabled. Port-forward the client to test locally:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "smartdoc.client.fullname" . }} 8080:{{ .Values.client.service.port }}
  # then open http://localhost:8080/
{{- end }}

Check rollout status:

  kubectl --namespace {{ .Release.Namespace }} get pods,svc,ingress -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if not .Values.db.existingSecret }}
{{- if eq .Values.db.password "postgres" }}

WARNING: the database is using the default development password "postgres".
Override it on the cluster with `--set db.password=...` or `--set db.existingSecret=...`.
{{- end }}
{{- end }}
